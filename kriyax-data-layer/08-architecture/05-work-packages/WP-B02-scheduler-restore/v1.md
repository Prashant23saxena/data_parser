# Work Package: WP-B02

**Title:** Scheduler restore and automatic execution
**Version:** v1
**Status:** PENDING
**Stream:** B
**Component:** backend-pipelines-service
**Estimated effort:** M
**CRITICAL PATH**

## What this WP delivers

Scheduled pipelines are restored on backend startup and run automatically when due.

## Inputs

- WP-B01
- Contract C-007
- Specs F-PS-002, F-PS-003, F-PS-004, F-PS-006

## Hard dependencies

- WP-B01 DONE.

## Soft dependencies

- Odoo pre-step can be skipped or mocked until WP-C02.

## Build steps

1. Add scheduler bootstrap from persisted pipeline JSON.
2. Skip automatic runs for disabled pipelines.
3. Trigger `run_pipeline_now(..., trigger="schedule")` when due.
4. Persist scheduled run records.
5. Recalculate `nextRunAt` after successful scheduled execution.
6. Add startup/restore tests using short fake schedules.

## Test invocation

```bash
cd /Users/shivanitidke/Desktop/Kriya_DataParser/kriyax-data-app/backend && .venv/bin/python -m pytest
```

## Acceptance checklist

- [ ] Scheduled jobs are restored from JSON on app startup.
- [ ] Enabled due jobs run automatically.
- [ ] Disabled jobs do not run automatically.
- [ ] Scheduled runs appear in run history with trigger `schedule`.
