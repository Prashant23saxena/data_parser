# Work Package: WP-E01

**Title:** Agent backend generate/correct API
**Version:** v1
**Status:** PENDING
**Stream:** E
**Component:** backend-agent-service
**Estimated effort:** L

## What this WP delivers

Backend endpoints generate Python/pandas code from schema context and propose corrections from tracebacks.

## Inputs

- Contract C-008
- Contracts C-003, C-005
- Specs F-AL-001, F-AL-002, F-AL-003, F-AL-005, F-AL-006

## Build steps

1. Add `/api/agent/generate`, `/correct`, and `/follow-up`.
2. Build schema-only context from catalog by default.
3. Add LLM provider configuration.
4. Enforce Python/pandas-only output using `load_table()` and `save_table()`.
5. Add max correction attempts and no auto-run behavior.
6. Add mocked-provider tests.

## Test invocation

```bash
cd /Users/shivanitidke/Desktop/Kriya_DataParser/kriyax-data-app/backend && .venv/bin/python -m pytest
```

## Acceptance checklist

- [ ] Schema context includes real table/column names.
- [ ] Generated code uses platform helpers.
- [ ] Correction consumes traceback and returns candidate code.
- [ ] Sample rows are excluded unless explicitly requested.
